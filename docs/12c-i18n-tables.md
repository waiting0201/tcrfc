# 12c — `*_i18n` 側表欄位清單（S0-3b）

> **用途**：定義 39 張目前只標 🌐、沒有逐欄定義的 `*_i18n` 側表欄位清單，補齊 `docs/12` §4「資料表總覽」留下的缺口
> （`docs/12a` §5.12：47 張 🌐 中只有 8 張寫明側表欄位）。**本檔是 `docs/12` §2 雙語策略的延伸，不是新規格**——
> 形狀（複合主鍵 `(<entity>_id, locale)`、`zh-Hant` 必存 `en` 可缺、刪除母列 cascade）一律沿用 `docs/12` §2.2。
>
> 🔴 **本檔不得引入規劃書沒有的規格。** 每一個列出的欄位都標了信心度與規劃書行號依據；
> 找不到依據的欄位一律不列，改在 §4／§5 說明推論理由或問題所在。
>
> 來源：主站規劃書 v3.11（`output/TCRFC_前後台功能規劃書.md`，1826 行）。行號皆指此檔。

---

## §1 判準

**欄位要放進側表，唯一的判準是「它會不會因語系而不同」**（沿用 `docs/12` §2.1／§2.2）：

| 判準 | 說明 |
|---|---|
| ✅ 進側表 | 名稱、標題、簡介、說明內文、按鈕與 CTA 文案、`seo_title`／`seo_description`、圖片 `alt`、任何給使用者看的文字 |
| ❌ 留主表 | `slug`、`status`、日期時間、數字、金額、布林、外鍵、排序、代碼、座標、物件儲存鍵（`*_key`／`*_width`／`*_height`） |

**本檔額外採用的三條紀律**（動手核對每張表時發現有必要明訂，否則會逐張各自為政）：

1. **主表 ERD 沒畫出來的欄位不等於不存在**——`docs/12a` 的側表「不入圖」是刻意的（§5.12 開頭即言明），
   所以拿主表 ERD 缺某欄位當作「規劃書沒寫」是錯的推論；本檔仍是以**規劃書原文**為準，只是拿 ERD 主表做交叉核對。
2. **只有規劃書寫「（中／英）」「雙語」「皆須中英」等字樣才算信心度「高」**；規劃書只列出欄位名稱、
   沒有語系標記的，算信心度「中」（功能描述到了，逐欄雙語與否要靠 `docs/04`／`docs/12` §5.2 的通則「所有具前台展示的型別皆需支援 zh/en」補足）。
3. **主表 ERD 已經放了某段文字型欄位在主表（不是側表）的，優先尊重 ERD 的既有決定**，本檔不重複建側表欄位
   （例如 `Match.opponent`／`Standing.team_name` 是自由文字留在主表，不進 `match_i18n`）。

---

## §2 已定義的 8 張（對照組，不重複定義）

| 側表 | 欄位 | 出處 |
|---|---|---|
| `article_i18n` | `title`、`summary`、`body(json)`、`seo_title`、`seo_description` | `docs/12` §2.2 範例、`docs/12a` §5.12 |
| `faq_i18n` | 未逐欄明列；依 B4 原文（行 1016）推定為 `question`、`answer` | `docs/12` §2.4／B4（行 1015–1022） |
| `match_i18n` | `opponent`、`venue` | `docs/12` §2.4（`Match.opponent_en`／`venue_en`，行 1251） |
| `member_draw_i18n` | `name`、`prize_description`、`rules`、`notes` | `docs/12` §2.4（K5，行 1287） |
| `membership_benefit_i18n` | `group_label`、`free_value`、`paid_value` | `docs/12` §2.4（行 673） |
| `partner_i18n` | 未逐欄明列；依 E1 原文（行 1102）至少含 `name` | `docs/12` §2.4（行 1259–1260） |
| `sponsor_i18n` | 未逐欄明列；依 E2 原文（行 1105）至少含 `name` | `docs/12` §2.4（行 1259–1260） |
| `setting_i18n` | 未逐欄明列；文案類設定值 | `docs/12` §2.4、§4.0（`Setting` 列） |

---

## §3 本次新定義的 39 張

> 信心度定義：**高**＝規劃書明文標「（中／英）」或「雙語」｜**中**＝規劃書列出欄位名稱但未標語系｜**低**＝只能從用途或 ERD 缺口推論。
> 信心度「低」的欄位集中列在 [§4](#4-信心度低的欄位)，不混在本節表格內。

### 3.0 共通機制

#### `EmailTemplate` → `email_template_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `subject` | `string(200)` | 中 | 行 817–822、1320：「僅五種系統信，皆須中英模板」，未逐欄列出主旨／內文 |
| `body` | `text` | 中 | 同上 |

> `EmailTemplate` 的 `type` 值域現為 9 個（會員 5 ＋ 商店 4，`docs/12` §4.0），每個 `template_code` 各一列，本表結構不受影響。

---

### 3.1 B 內容管理

#### `Page` → `page_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `seo_title` | `string(200)` | 中 | 行 1000–1004：「每頁具備：…SEO 設定…」，未逐欄列名，命名比照 `article_i18n` |
| `seo_description` | `string(300)` | 中 | 同上 |

> ⚠️ **沒有列出 `title`／`h1` 欄位。** `docs/12a` §5.1 的 `page` 主表 ERD 完全沒有標題欄位——內文與標題全部走
> 區塊化編輯器（`PageBlock`），規劃書也只寫「區塊化編輯器」沒有另外的頁面標題欄位。見 [§5](#5-發現的問題)。

#### `PageBlock` → `page_block_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `content` | `json` | 高 | 行 1000–1004（區塊化編輯器 13 種型別）＋ `docs/12` §5.2「所有具前台展示的型別皆需支援 zh/en」＋ 比照 `article_i18n.body json` 既有模式 |

> `content` 沿用「只存不查」紀律（`docs/12` §1.4 第 2 項）；**每語系一列**，避免像並排欄位那樣把 zh／en 兩份 JSON 塞進同一欄。

#### `ArticleCategory` → `article_category_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(64)` | 高 | 行 395–408（7.1–7.8 表格逐列給出中英分類名，如「7.1 Club News 俱樂部新聞」） |

#### `Tag` → `tag_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(64)` | 中 | 行 1006（B2「標籤、核心價值標籤」），未給出具體標籤名稱或語系標記 |

#### `PressResource` → `press_resource_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `title` | `string(200)` | 高 | 行 1034：「資源 CRUD：標題與說明（**雙語**）…」 |
| `description` | `text` | 高 | 同上 |

#### `Banner` → `banner_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `title` | `string(200)` | 高 | 行 280（Hero「支援標題／副標／雙 CTA」）＋行 1011–1014（B3「標題、CTA」） |
| `subtitle` | `string(300)` | 高 | 行 280 |
| `cta_1_label` / `cta_1_url` | `string(64)` / `string(500)` | 中 | 行 280「雙 CTA」，結構（兩組各含文案與連結）為推論，非逐欄列名 |
| `cta_2_label` / `cta_2_url` | `string(64)` / `string(500)` | 中 | 同上 |

#### `FaqCategory` → `faq_category_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(64)` | 中 | 行 558–567：「主題分類導覽卡：加入球隊、學院招生…」10 個分類，**只給中文名稱，沒有對應英文** |

---

### 3.2 C 球隊管理

#### `Competition` → `competition_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(64)` | 高 | 行 1463：「代號、**名稱（中／英）**、類型…」 |
| `organizer` | `string(128)` | 高 | 行 1463：「代號、名稱（中／英）、類型（對應 `Match.competition` 四值）、所屬球季、**主辦單位**、排序、啟用狀態」（S0-3d 新增；未標「（中／英）」，比照 `Sponsor`／`Charity` 等機構名稱一律走側表的既有慣例） |

> ✅ **已定案（2026-09-20）：走側表**，`docs/12a` 的 ERD 已移除主表的 `name_zh`／`name_en`。與其餘 37 張一致。

#### `Season` → `season_i18n`

**規劃書全文找不到任何 `Season` 的文字型欄位**（`docs/12` §12 踩雷點 17 也說明 `Season` 是本檔新增、規劃書只在關聯欄提到）。
無欄位可列。是否該標 🌐，見 [§5](#5-發現的問題)。

#### `Team` → `team_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(64)` | 中 | 行 1043：「球隊資料：…名稱…簡介…」，未標語系 |
| `intro` | `text` | 中 | 同上 |

#### `Player` → `player_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(64)` | 高 | 行 1053：「基本資料：**中英姓名**…」 |
| `bio` | `text` | 中 | 行 1054：「生涯資料：經歷、前東家、成就」，未標語系 |

#### `Staff` → `staff_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(64)` | 中 | 行 1060–1062：「教練：姓名、職稱…」「團隊成員：…簡介」，未標語系 |
| `title` | `string(64)` | 中 | 同上 |
| `bio` | `text` | 中 | 同上（含「專長、經歷」與「簡介」） |

#### `Standing` → `standing_i18n`

**無欄位可列。** `docs/12a` §5.2 明訂 `standing.team_name` 是主表自由文字（「對手隊名是自由文字不是 `Team`」），
規劃書 C4（行 1063–1068）也只寫「積分榜：手動維護表格」，沒有任何雙語欄位描述。標 🌐 可能是誤標，見 [§5](#5-發現的問題)。

#### `Achievement` → `achievement_i18n`

**無欄位可列。** `docs/12a` §5.2 的 `achievement` 主表把 `competition_name`／`placing` 都放在主表（非側表），
規劃書 C5（行 1070）只寫「年份、賽事、名次、關聯球隊」，沒有雙語標記。標 🌐 可能是誤標，見 [§5](#5-發現的問題)。

#### `Milestone` → `milestone_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `title` | `string(200)` | 高 | 行 1071：「里程碑（2.8）：日期、**標題**、描述、圖片…」＋行 302（2.8 前台「事件描述」） |
| `description` | `text` | 高 | 同上 |

---

### 3.3 P 課程與活動

#### `Program` → `program_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(128)` | 高 | 行 1079：「欄位：**名稱**、**簡介**…課程內容（區塊編輯）…」 |
| `intro` | `text` | 高 | 同上 |
| `content` | `json` | 高 | 同上（「課程內容（區塊編輯）」，比照 `PageBlock` 走區塊 JSON） |

#### `Session` → `session_i18n`

**無欄位可列。** 行 1082：「梯次：期間、上課時間表、地點、名額上限…費用…狀態」全部是日期／數字／狀態，
`docs/12a` §5.4 的 `session` 主表也完全沒有文字型欄位。標 🌐 可能是誤標，見 [§5](#5-發現的問題)。

#### `Trial` → `trial_i18n`

無欄位可高信心列出，見 [§4](#4-信心度低的欄位)（`audience` 一欄）。

---

### 3.4 E 商業模組

#### `Sponsor` → `sponsor_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `content` | `text` | 高 | 行 1105：「贊助商：名稱、Logo、等級（主贊助／官方贊助／支持夥伴）、合約期間、**贊助內容**、聯絡窗口、到期提醒」（S0-3d 新增） |

> `name` 欄位已見 [§2](#2-已定義的-8-張對照組不重複定義)（對照組，未逐欄明列），本節只補「贊助內容」這一新增欄位，不重複列出 `name`。
> 聯絡窗口（`contact_name`／`contact_phone`／`contact_email`）已在 `docs/12a` §5.5 的 `sponsor` 主表，非語系欄位，不進本側表。

#### `SponsorPackage` → `sponsor_package_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(128)` | 高 | 行 1479：「贊助方案（9 種）：**名稱（中／英）**、方案內容、權益清單、適合對象…」 |
| `content` | `text` | 中 | 同上（「方案內容」未標語系） |
| `benefit_list` | `text` | 中 | 同上（「權益清單」未標語系，型別暫定文字非結構化陣列） |
| `audience` | `string(128)` | 中 | 同上（「適合對象」未標語系） |

#### `Proposal` → `proposal_i18n`

**不建議建立。** `docs/12a` §5.5 的 `proposal.title` 是**主表的單一欄位**（`string_128`，非側表），
多語言需求已由子表 `ProposalFile(locale, file_key)` 承載（每語系各自一份 PDF）。
行 1111「上傳提案 PDF（可多版本／多語系）」講的是 PDF 檔案本身的語系，不是 `Proposal` 標題要雙語。
`docs/12` §4.4 標 `Proposal` 🌐 可能是把「多語 PDF」誤讀成「側表」，見 [§5](#5-發現的問題)。

---

### 3.5 F 文化模組

#### `ComicCharacter` → `comic_character_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(64)` | 高 | 行 1128：「角色管理：**角色名**、**設定**、圖片…」＋ `docs/12a` §5.5b 明文「角色名與設定…走 `*_i18n` 側表」 |
| `description` | `text` | 高 | 同上（「設定」＝角色背景設定） |

#### `ComicEpisode` → `comic_episode_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `title` | `string(128)` | 高 | 行 1129：「集數管理：集數編號、**標題**…」＋ `docs/12a` §5.5b「集數標題走 `*_i18n` 側表」 |

#### `FanEvent` → `fan_event_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(128)` | 中 | 行 1135：「球迷活動：活動 CRUD…」未逐欄列名；`docs/12a` §5.6 的 `fan_event` 主表無名稱欄位，確認走側表 |
| `description` | `text` | 中 | 同上（活動說明為 CRUD 常見欄位，非規劃書逐字列出） |

---

### 3.6 G 表單與詢問

#### `Form` → `form_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `auto_reply_body` | `text` | 中 | 行 1144：「每張表單可設定：…**自動回覆信模板**…」，未標語系，比照 `EmailTemplate` 處理 |

> ⚠️ **沒有列出表單「名稱」／「標題」欄位。** `docs/12a` §5.4b 的 `form` 主表只有 `form_code`（代碼），
> 7 類表單的顯示名稱在規劃書 3.10（行 494–522）以固定表格列出（如「10.1 Join as a Player」），
> 較像是**寫死的介面文案（`UiString` 範疇）而非逐筆資料**，是否仍需要 `form_i18n.name` 待確認，見 [§5](#5-發現的問題)。

#### `FormField` → `form_field_i18n`

無欄位可高信心列出，見 [§4](#4-信心度低的欄位)（`label`／`placeholder`）。

---

### 3.7 I 網站設定

#### `MenuItem` → `menu_item_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `label` | `string(64)` | 中 | 行 1178：「選單管理：主選單／Mega Menu／Footer 選單，拖曳排序、多層級、外部連結、**雙語**」——**雙語**為明文，但欄位名（`label`／`title`／`name`）本檔推論命名 |

#### `Venue` → `venue_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(128)` | 中 | 行 1183：「場地管理：**場地名稱**、地址、經緯度、交通說明、照片」，未標語系 |
| `address` | `string(255)` | 中 | 同上 |
| `directions` | `text` | 中 | 同上（「交通說明」） |

---

### 3.8 J 系統管理

#### `Club` → `club_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(64)` | 高 | 行 1216：「俱樂部資料：代號、**名稱與簡介（中／英）**…」 |
| `description` | `text` | 高 | 同上 |

> ✅ **已定案（2026-09-20）：走側表**，`docs/12a` 的 ERD 已移除主表的 `name_zh`／`name_en`。
> 規劃書明文要求的「簡介（中／英）」（行 1216）因此有了落點——就是本表的 `description`。

---

### 3.9 K 會員管理

#### `MembershipPlan` → `membership_plan_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(64)` | 高 | 行 1242：「方案設定：…**方案名稱（中／英）**…權益說明…」 |
| `benefit_note` | `text` | 中 | 同上（「權益說明」未標語系；注意條目層級的權益已由既有的 `membership_benefit_i18n` 承載，此為方案層級的概述文字） |

#### `PartnerStore` → `partner_store_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(128)` | 高 | 行 1257：「特約店家 CRUD：**名稱（中／英）**、照片／Logo、類別、**地址（中／英）**…**優惠內容（中／英）**…」 |
| `address` | `string(255)` | 高 | 同上 |
| `offer_content` | `text` | 高 | 同上 |

---

### 3.10 L 行事曆管理

#### `CalendarCustomEvent` → `calendar_custom_event_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `title` | `string(128)` | 高 | 行 1364：「欄位：**標題（中／英）**、所屬隊別…說明…」 |
| `description` | `text` | 中 | 同上（「說明」未標語系，但緊接在明文雙語的 `title` 之後） |

#### `EventType` → `event_type_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(64)` | 中 | 行 1369：「賽事類型維護：聯賽／盃賽／友誼賽等**名稱**、識別色、圖示…」，未標語系 |

---

### 3.11 S 商店

#### `Collection` → `collection_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(64)` | 中 | 行 1395（S1「Collection 分類（俱樂部／學院／球迷）」），分類本身名稱未標語系 |
| `narrative` | `text` | 高 | 行 1399：「系列介紹文：各 Collection 的**品牌敘事區塊**」 |

#### `Product` → `product_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(128)` | 高 | 行 1395：「商品：**名稱（中／英）**…商品敘事（中／英）…」 |
| `narrative` | `text` | 高 | 同上 |
| `seo_title` | `string(200)` | 高 | 同上：「…排序、**SEO 欄位（標題／描述／結構化資料）**」 |
| `seo_description` | `string(300)` | 高 | 同上 |
| `tags` | `string(255)` | 高 | 行 1396：「商品：名稱（中／英）、Collection 分類（俱樂部／學院／球迷）、**標籤**、圖集…」（S0-3d 新增；標籤為前台可見的商品分類文案，比照其他客戶端可見文字欄位走側表，與 `Enquiry.tags` 內部用途不同，見 `docs/12a` 該欄位的判斷說明） |

> `size_chart`（尺碼表）未列入——見 [§4](#4-信心度低的欄位)。`size_chart` 本身（非語系版本）已於 S0-3d 補進 `docs/12a` §5.8 `product` 主表（`json size_chart`），僅存放結構化尺寸數據；是否需另建 i18n 版本仍待人工核實，見 [§4](#4-信心度低的欄位)。

---

### 3.12 B6 慈善內容（主站）

#### `Charity` → `charity_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(128)` | 中 | 行 1026：「公益團體資料：**團體名稱**、**簡介**、Logo 或代表圖、官網連結、聯絡窗口…」，未標語系 |
| `intro` | `text` | 中 | 同上 |

#### `CharityProgram` → `charity_program_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(128)` | 中 | 行 1024：「慈善計畫：**名稱**、封面、對象、期間、狀態…緣起與內容（區塊編輯）…**捐助內容**…」，未標語系 |
| `target_audience` | `string(200)` | 中 | 同上（「對象」） |
| `content` | `json` | 中 | 同上（「緣起與內容（區塊編輯）」，比照 `Program.content`） |
| `donation_content` | `text` | 中 | 同上（「捐助內容」） |

#### `ImpactRecord` → `impact_record_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `donation_content` | `text` | 高 | 行 1025：三項必填之一，「**捐助內容**（文字描述，如「足球 50 顆…」）」 |
| `location` | `string(128)` | 中 | 同上（「另含日期、**地點**、簡述…」，未標語系） |
| `brief_description` | `text` | 中 | 同上（「**簡述**」） |

> ⚠️ **三項必填的第一項「公益團體名稱」不進本側表**——`docs/12a` §5.9 的 `impact_record` 主表已有 `charity_id` 外鍵，
> 團體名稱透過關聯到 `Charity`／`charity_i18n` 取得，不重複儲存。
> 🔴 **`location`／`brief_description`／`donation_content` 三欄在 `docs/12a` §5.9 的 `impact_record` 主表 ERD 完全沒有出現**
> （主表只有 `image_key`／`image_width`／`image_height`／`happened_on` 四個非鍵欄位），不只是「側表沒畫出來」的正常省略，
> 是**規劃書要求的三個文字欄位在整份 ERD 裡找不到落點**，見 [§5](#5-發現的問題)。

#### `ImpactMetric` → `impact_metric_i18n`

| 欄位 | 型別 | 信心度 | 依據 |
|---|---|---|---|
| `name` | `string(64)` | 中 | 行 1027：「影響力數據：可自訂統計項目（**名稱**、單位、數值、是否公開）」，未標語系 |
| `unit` | `string(16)` | 低 | 同上（「單位」，如「人」「場」「元」，是否需要雙語見 [§4](#4-信心度低的欄位)） |

---

## §4 信心度低的欄位

以下欄位**不進 §3 的正式表格**，因為規劃書沒有逐欄列出，只能從用途或 ERD 缺口推論。列出是為了不遺漏候選，
**轉 DDL 前必須由人工核實，本檔不擅自升級信心度**。

| 表 | 推論欄位 | 推論理由 |
|---|---|---|
| `Trial` | `audience`（對象說明） | 行 1095「對象」一詞存在，但 `docs/12a` §5.4 的 `trial` 主表完全沒有對應欄位（只有 `team_id`／`venue_id`／`trial_on`／`capacity`／`deadline_on`），不確定「對象」最終是自由文字、`Team` 的年齡層推導，還是純粹遺漏 |
| `FormField` | `label`（欄位顯示名稱）、`placeholder`（提示文字） | 行 1143 只列出欄位**型別**（文字、下拉、多選…），沒有提到欄位標籤需要雙語；但動態表單欄位若不能雙語顯示，無法達成「所有具前台展示的型別皆需支援 zh/en」的通則 |
| `ImpactMetric` | `unit`（單位） | 多數統計單位（人、場、元、公斤）中英本就不同，但規劃書未明確要求本欄位雙語，也可能維持通用符號不必翻譯 |
| `Product` | `size_chart`（尺碼表內容） | 行 1395 列出「尺碼表」但未標語系；若為純數字表格可能不需要 i18n，若含文字說明（如「亞洲尺碼偏小，建議大一號」）則需要。無法判斷具體形式 |
| `Season` | （無推論欄位） | 規劃書全文查無任何文字型描述，連低信心度的候選都找不到，見 [§5](#5-發現的問題) |

---

## §5 發現的問題

**這一節不擅自調和，只如實記錄。**

1. **`Club`／`Competition` 的雙語欄位寫法與 `docs/12` §2.1 的側表原則直接衝突**（規劃書要求分別如此）：
   - `docs/12a` §5.2 的 `competition` 主表把 `name_zh`／`name_en` 直接放在主表；
   - `docs/12a` §5.11 的 `club` 主表同樣把 `name_zh`／`name_en` 放在主表。
   `docs/12` §2.3 明列的「不走側表」例外只有**快照表**、**後台專用表**（`AdminRole`／`Permission`）、**`UiString`**三種，
   `Club` 與 `Competition` 都不屬於這三種——它們是**前台可見的內容主體**（`Club` 對應俱樂部品牌頁與站台識別、
   `Competition` 對應賽事系列名稱），理論上應該和 `Team`／`Player` 一樣走側表。**本檔不判定哪一個寫法才對**，
   只指出：若維持 ERD 現狀（主表並排欄位），則與 §2.1 論證的「加第三語系＝改 DDL」問題會在這兩張表重演；
   若改為側表，`docs/12a` 的兩張 ERD 圖需要同步修正。

2. **`Club.description`（簡介）完全遺失**——不是側表 vs 主表的路線之爭，是**規劃書明文要求的欄位在 ERD 完全找不到**。
   規劃書行 1216 寫「名稱**與簡介**（中／英）」，但 `docs/12a` §5.11 的 `club` 主表沒有 `description`／`intro`
   任何對應欄位，本檔起草的 `club_i18n` 也是本次新補的，**代表這欄位過去被漏掉，不是刻意不做**。

3. **`impact_record` 的三個文字欄位（`location`／`brief_description`／`donation_content`）在整份 ERD 都不存在**。
   規劃書行 1025 明文列出「另含日期、地點、簡述」與必填的「捐助內容」，但 `docs/12a` §5.9 的 `impact_record`
   主表只有圖片與日期欄位。這比「該不該走側表」更根本——**這三個欄位無論放主表或側表都還沒有落點**。

4. **`Standing`／`Achievement`／`Session`／`Season` 四張表標了 🌐，但規劃書與既有 ERD 都找不到任何語系相關欄位**：
   - `Standing.team_name`、`Achievement.competition_name`／`placing` 已經是 ERD 主表的自由文字欄位，不是側表候選；
   - `Session` 全部欄位是日期／數字／狀態；
   - `Season` 規劃書全文沒有描述任何文字欄位，只在關聯欄被提及（`docs/12` §12 第 17 點也承認 `Season` 是本檔新增）。
   **這四張很可能是 🌐 標記過寬**——建議回頭核對 `docs/12` §4.2／§4.3 這四列，若沒有人補得出欄位就拿掉 🌐。

5. **`Proposal` 的多語需求已經由 `ProposalFile(locale, file_key)` 承載**，`proposal.title` 在 ERD 主表是單一欄位。
   規劃書行 1111「多版本／多語系」指的是 PDF 檔案本身，不是 `Proposal` 這筆資料要有雙語標題。
   `docs/12` §4.4 標 `Proposal` 🌐 可能是把「檔案多語」誤讀成「側表多語」，建議確認後拿掉 🌐 或改標注清楚理由。

6. **`Form` 的顯示名稱來源不明確**。規劃書 3.10（行 494–522）用固定表格列出 7 類表單的中英名稱
   （如「10.1 Join as a Player 加入球隊」），這些名稱看起來更像**寫死的介面文案**（`UiString` 範疇，`docs/12` §2.3
   第三個例外），而不是後台可編輯的逐筆資料。若 `Form` 真的需要後台可編的顯示名稱，才需要 `form_i18n.name`；
   若名稱本來就固定不給後台改，則不需要側表、`docs/12` 標 🌐 也是過寬。**本檔兩案並陳，不代為決定。**

7. **翻譯時態**：本檔沿用 `docs/12` §2.5「零列即缺英文」的判定方式，但 `Club`／`Competition` 若最終維持並排欄位
   （問題 1），則無法套用同一套「翻譯狀態總覽以 `LEFT JOIN` 側表」的查詢邏輯，需要另外為這兩張表寫特例查詢——
   這是問題 1 沒解決前，會連帶卡住 I 模組「翻譯狀態總覽」實作的下游影響，一併記錄於此。
