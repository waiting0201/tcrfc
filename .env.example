# .env.example — docker-compose.yml／docker-compose.dev.yml 用的環境變數範本
#
# 用法：cp .env.example .env，再填真實值。.env 本身不納版控（.gitignore 已排除，見那份檔案的
# 「開發相依與建置產物」段）。
#
# 🔴 本檔只放「這兩份 compose 檔本身要用」的變數（網域、映像檔 tag、Redis／本機資料庫密碼）。
# 應用程式真正的機密（LINE Pay 憑證、JWT 簽章金鑰、資料庫連線字串、發票金鑰……）**不放這裡**：
#   - 正式環境：docs/20-cicd.md §7.2，放 VM 的 /opt/tcrfc/secrets/club.env｜charity.env（不進 git）
#   - 本機開發：deploy/dev/club.env｜charity.env（範本在 deploy/dev/*.env.example，同樣不進 git）
# 這個切分本身就是 docs/20 §7.2 的設計原則——能不進 GitHub Secrets／不進這個公開 repo 的機密，
# 一律留在它該待的地方。
#
# 對應 docs/20-cicd.md §3（五個映像檔）、§7（Secrets 清單）；docs/17-deployment.md §1（八個容器）。

# ── 映像檔來源（docs/20-cicd.md §2：GHCR，套件公開，VM 不需要登入憑證） ──────────────
GHCR_OWNER=waiting0201
# 部署一律用 git SHA 當 tag（docs/20-cicd.md §6），這裡的 latest 只給本機手動測試用預設值；
# 正式部署由 deploy.yml 覆寫成實際的 SHA，不依賴這個預設值。
IMAGE_TAG=latest

# ── 環境旗標（docs/17-deployment.md §10.4，nuxt-club／nuxt-charity 用） ──────────
# ⚠️ 這是「同一套正式環境的兩個階段」，不是兩套環境——本專案只有本機開發（docker-compose.dev.yml）
# 與正式 VM（docker-compose.yml）兩套環境，沒有 staging 環境（docs/20-cicd.md §1）。
# prelaunch：三個公開前台的 robots.txt 全擋、加 X-Robots-Tag noindex（蓋掉 GEO-02 正式規則）。
# production：GEO-02 正常規則生效（全站允許 AI 爬蟲，但排除會員區等路徑）。
# 🔴 切正式時務必改成 production 並重啟——忘記改的後果是正式站被自己擋掉索引，
# 比忘記設 prelaunch 的後果更容易被發現（流量掉到零很明顯），但仍會拖慢上線後的收錄時間。
SITE_ENV=prelaunch

# ── 網域（依 docs/17 §1／§3／§10，proxy 依 Host 分流） ───────────────────────────
# 🔴 正式網址到位前直接使用下方六個值（tcrfc.tw 子網域，DNS 已自控，見 docs/17-deployment.md §10.1）。
# 正式網址到位後，把這六個值換成正式網域再重啟——藍鯨與慈善的正式網域尚未確定
# （STATUS.md 阻塞清單 B-4／B-7）；主站是 tcrfc.tw 還是 www.tcrfc.tw 也尚未定案
# （docs/17-deployment.md §10.6，apex 與 www 的取捨），下方先用 apex，定案後可能要改。
TCRFC_DOMAIN=stg.tcrfc.tw
BW_DOMAIN=bw-stg.tcrfc.tw
CHARITY_DOMAIN=charity-stg.tcrfc.tw
ADMIN_WEB_DOMAIN=admin-stg.tcrfc.tw
ADMIN_CHARITY_DOMAIN=admin-charity-stg.tcrfc.tw
API_DOMAIN=api-stg.tcrfc.tw

# 正式網址範例（定案後改用這組，不要兩組同時取消註解）：
# TCRFC_DOMAIN=www.tcrfc.tw
# BW_DOMAIN=<藍鯨正式網域，待 B-4 解除>
# CHARITY_DOMAIN=<慈善正式網域，待 B-7 解除>
# ADMIN_WEB_DOMAIN=admin.tcrfc.tw
# ADMIN_CHARITY_DOMAIN=<依慈善正式網域決定>
# API_DOMAIN=api.tcrfc.tw

# Let's Encrypt 到期與帳號通知信箱（deploy/Caddyfile 的 email 指令）
ACME_EMAIL=ops@example.tw

# ── proxy 設定檔路徑（docs/17 §10.4） ──────────────────────────────────────────
# 不設＝用正式版 deploy/Caddyfile。正式網址到位前改指向 deploy/Caddyfile.prelaunch，
# 三個公開前台就會多一道 HTTP Basic Auth。這是「換 .env 的值就換階段」，不是換環境，
# 所以沒有第二份 compose 檔——切正式時把下面這行註解掉、重跑 docker compose up -d 即可。
CADDYFILE=./deploy/Caddyfile.prelaunch

# ── 正式網址到位前的 Basic Auth（只在 CADDYFILE 指向 .prelaunch 時生效，docs/17 §10.4） ──
# 🔴 只給知道要看的人（開發團隊、客戶窗口），不得公開張貼、不得與任何後台帳密相同。
# PRELAUNCH_BASIC_AUTH_HASH 用「caddy hash-password --plaintext '<密碼>'」產生，不要填明文密碼。
# ⚠️ 掛了 .prelaunch 卻沒填值，Caddy 會啟動失敗——刻意的，見 deploy/Caddyfile.prelaunch 檔頭。
PRELAUNCH_BASIC_AUTH_USER=change-me
PRELAUNCH_BASIC_AUTH_HASH=change-me-run-caddy-hash-password

# ── Redis（docs/17 §1，只服務俱樂部，慈善不得接觸） ─────────────────────────────
REDIS_PASSWORD=change-me-strong-random-password

# ── 僅本機開發（docker-compose.dev.yml 的 mssql-dev 服務） ───────────────────────
# 🔴 只用於本機，絕對不要填正式密碼、也不要把這個值帶去任何正式環境的變數。
MSSQL_DEV_SA_PASSWORD=ChangeMe_Local123!
