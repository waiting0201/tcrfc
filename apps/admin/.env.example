# apps/admin 本機開發用環境變數範本。複製成 .env.development 後依需要調整（該檔不納版控，
# 見根目錄 .gitignore）。不建這個檔案時，程式碼一律退回下面這個預設值，本機開發不必先建立
# 這個檔案就能動——跟 apps/api/README.md「本機開發若忘記帶 CORS_ALLOWED_ORIGINS」是同一種設計。

# apps/api（.NET）的基底網址。本機 dotnet run 的預設監聽位址見 apps/api/README.md。
VITE_ADMIN_API_BASE_URL=http://127.0.0.1:5299
